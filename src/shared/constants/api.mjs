const port = '8080'

const API = {
    ADRESS_BACKEND: 'http://localhost' + ':' + port + '/api/v1/',
    SIGNUP: 'signup',
    SIGNIN: 'signin',
    GET_POST_LIST: 'post/get_list'
}

export default API;
