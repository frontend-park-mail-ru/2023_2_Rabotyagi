<button class='card'>
    <div class='badges'>
        {{#if this.badges.safeDeal}}
            {{!-- {{this.badges.safeDeal}} --}}
        {{/if}}

        {{#if this.badges.delivery}}
            <div class='badge'>
                {{!-- {{this.badges.delivery}} --}}
            </div>
        {{/if}}

        {{#if this.badges.city}}
            <div class='badge'>
                <span class="text-regular">{{this.badges.city}}</span>
            </div>
        {{/if}}
    </div>
    <div class='card-body'>
        {{#if this.img}}
            <img class='card-img' src='{{this.img}}' />
        {{else}}
            <div class="card-img"></div>
        {{/if}}
        <div class='card-info'>
            <span class='text-regular'>{{this.cardInfo.price}} ₽</span>
            <span class='text-regular card-info-title'>{{this.cardInfo.title}}</span>
        </div>
        {{! <div class="card-fav-btn"></div> }}
    </div>
    </div>